export interface IBottomFooter {
    upstairs: string,
    groundFloor: [
        {
            first: string,
            pages: [
                {
                    title: string
                }
            ]
        }
    ]
}