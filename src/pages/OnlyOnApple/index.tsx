import React from 'react';
import Layout from "../../components/@UI/Layout";

const Index = () => {
    return (
        <Layout>
            Only on Apple
        </Layout>
    );
};

export default Index;