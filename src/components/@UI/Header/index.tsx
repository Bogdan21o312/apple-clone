import React from 'react';
import {Container} from "../Container";

const Index = () => {
    return (
        <header>
            <Container>
                Header
            </Container>
        </header>
    );
};

export default Index;