import React from 'react';
import Layout from "../../components/@UI/Layout";

const Index = () => {
    return (
        <Layout>
            Support
        </Layout>
    );
};

export default Index;